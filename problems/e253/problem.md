# e253. a + b problem

## 內容

```
#include 

int add(int a, int b) {
    while(b) {
        int ta = a _ b, tb = (a _ b) __ 1;
        a = __, b = __;
    }
    return a;
}

int main() {
    int a, b;
    scanf("%d %d", &a, &b);
    printf("%d", add(a, b));

    return 0;
}
```

 

請把上述程式的'_'填完，讓add()可以回傳a+b的結果。


一個'_'代表一個字元


#不准使用+ - * /運算子

## 輸入說明

(本題無輸入)

## 輸出說明

依照從上到下、從左到右的順序輸出填入的答案，並用空白隔開


ex:


/ * && gg cc

## 範例輸入 #1

```
(本題無輸入)
```

## 範例輸出 #1

```
(答案被帕克吃掉了)
```

#include <iostream>
#include <vector>
using namespace std;

int main(){
    int n,m,a,b;
    cin >> n >> m;
    vector<int> ans(n);
    for(int i=0;i<m;i++){
        cin >> a >> b;
        switch(b){
            case -1:
                break;
            case -2:
                break;
        }
    }
    return 0;
}
#include <iostream>
#include <vector>

using namespace std;

int main() {
    int N;
    while (cin >> N) {
        vector<int> arrivals(N);
        for (int i = 0; i < N; ++i) {
            cin >> arrivals[i];
        }

        int current_time = 0;
        int last_patient_end_time = 0;
        
        // 黑傑克在第 0 分鐘抵達，初始等待計時器從 0 開始算
        // 但規則 4 是「連續三十分鐘沒有病患候診」才離開
        // 這意味著如果他在時間 T 結束看診或等待，下一個病患必須在 T+30 之前（不含 T+30）出現
        
        for (int i = 0; i < N; ++i) {
            int arrival = arrivals[i];
            
            if (arrival >= current_time) {
                // 病患到達時，醫生已經有空檔了
                // 檢查空檔是否超過 30 分鐘
                if (i > 0 && arrival - last_patient_end_time >= 30) {
                    // 超過 30 分鐘沒人了，醫生在 last_patient_end_time + 30 離開
                    current_time = last_patient_end_time + 30;
                    break;
                }
                
                // 醫生開始看這個病人
                current_time = arrival + 5;
                last_patient_end_time = current_time;
            } else {
                // 病患到達時，醫生還在忙，病人要排隊
                // 醫生看完目前的病人後立刻看下一個
                current_time += 5;
                last_patient_end_time = current_time;
            }
        }

        // 如果所有病人都看完了，且沒有中途離開
        // 題目問的是第幾分鐘離開
        // 如果是因为看完所有病人而自然結束，離開時間就是最後一個病人看完的時間嗎？
        // 不，題目範例說：若連續 30 分鐘沒有病患，便結束。
        // 如果看完最後一個病人後，再也沒人來了，那他什麼時候離開？
        // 題目描述：「若連續三十分鐘(不含三十分鐘整)沒有病患候診，便結束當地的行程」
        // 如果最後一個病人在 time T 看完，之後沒人來，那他在 T+30 離開。
        // 但範例 1 中，第三個病患在 41 分來，醫生在 10 分看完第二個，等到 40 分（共 30 分鐘），於是在 40 分？還是 41 分離開？
        // 範例說明：「等待 30 分鐘 (第 11 至 40 分鐘)，由於都沒有病患前來診所看診，便結束看診，並於第 41 分鐘啟程前往下一間診所。」
        // 這裡的邏輯有點微妙。
        // 第 10 分鐘看完。
        // 第 11, 12, ..., 40 分鐘這 30 分鐘期間沒人。
        // 規則說「連續三十分鐘...沒有病患」，這裡過了 30 分鐘（11~40 正好 30 分鐘）。
        // 然後說「於第 41 分鐘啟程」。
        // 這表示如果在 time T 結束看診，下一個病患在 time A 到來。
        // 如果 A - T > 30，則醫生在 T + 30 時刻結束等待，並在 T + 30 + 1 ? 或者就是 T+30?
        // 範例：T=10, A=41. A-T = 31. 31 > 30. 醫生離開時間是 41? 
        // 範例輸出是 41。
        // 說明寫「於第 41 分鐘啟程」。這暗示離開時間等於那個錯過病人的到達時間？
        // 不，邏輯應該是：醫生會等到「連續 30 分鐘沒人」這個條件觸發。
        // 從第 11 分鐘開始算沒人，到第 40 分鐘結束，剛好 30 分鐘。
        // 一旦滿 30 分鐘沒人，他就走了。他走的時間點是第 40 分鐘結束的那一刻？還是第 41 分鐘開始？
        // 範例說「於第 41 分鐘啟程」，輸出 41。
        // 另一種解讀：只要下一個病人的到達時間 A >= 當前結束時間 T + 30，醫生就會在 T + 30 離開？
        // 如果 A = 40, T = 10. A - T = 30. 規則說「不含三十分鐘整」。所以如果剛好 30 分鐘（A=40），醫生不會走，會看診。
        // 如果 A = 41, T = 10. A - T = 31 > 30. 醫生會走。
        // 醫生什麼時候走？範例說是 41。這很奇怪，通常應該是 10+30 = 40 就走人了。
        // 但範例明確說「於第 41 分鐘啟程」，且輸出 41。
        // 讓我們再讀一次：「若連續三十分鐘(不含三十分鐘整)沒有病患候診，便結束...」
        // 也許意思是：醫生會觀察，如果發現下一個病人來的時候，距離上次結束已經超過 30 分鐘了，那他其實在病人來的當下才發現自己早就該走了？
        // 不，這不符合模擬邏輯。
        // 讓我們重新解讀範例說明：
        // "等待 30 分鐘 (第 11 至 40 分鐘)" -> 這段時間長度是 30 分鐘。
        // "由於都沒有病患前來...便結束看診，並於第 41 分鐘啟程"
        // 這暗示離開時間 = 最後一個看診結束時間 + 30 + 1 ? 或者是 下一個病人的到達時間？
        // 如果下一個病人是 41 來，醫生 41 走。
        // 如果下一個病人是 100 來呢？醫生是 41 走還是 100 走？
        // 根據規則「若連續三十分鐘...沒有病患...便結束」，應該是一旦滿足條件就結束，不需要等病人來確認。
        // 所以如果是 100 來，醫生應該在 10+30 = 40 分鐘時就決定走了。
        // 但為什麼範例說是 41？
        // 可能範例的「第 41 分鐘啟程」是因為剛好有個病人在 41 來，題目想表達的是「當病人來的時候發現醫生走了」？
        // 不，題目問的是「黑傑克在第幾分鐘時離開診所」。
        // 如果按照嚴格模擬：
        // Time 10: 看完。
        // Time 11-40: 沒人。Count = 30 mins.
        // Time 40 結束時，滿足「連續 30 分鐘沒人」。
        // 那麼離開時間應該是 40 還是 41？
        // 範例輸出 41。
        // 讓我們嘗試另一種邏輯：離開時間 = max(最後看診結束時間 + 30, 下一個病人的到達時間) 如果下一個病人導致超時？
        // 不，這太複雜。
        // 讓我們再看一次條件：「不含三十分鐘整」。
        // 意思是間隔必須 **大於** 30 分鐘才會觸發離開。
        // 如果間隔是 30 分鐘（例如 10 結束，40 來），則 **不** 離開，繼續看診。
        // 如果間隔是 31 分鐘（例如 10 結束，41 來），則 **會** 離開。
        // 問題是：離開的時間點是什麼？
        // 如果是 41 來，醫生在 41 離開？那如果病人是 100 來呢？醫生難道要等到 100 才離開？
        // 不合理。醫生應該在 10+30 = 40 的時候就離開了。
        // 但範例輸出是 41。這只有一種可能：題目的意思是，醫生會在「發現」沒人來的時候離開，而這個發現的時刻是下一個病人到達的時刻？
        // 或者，範例的 41 只是巧合？
        // 讓我們假設邏輯是：如果下一個病人到達時間 A > last_end + 30，則醫生在 last_end + 30 離開。
        // 那範例應該是 40。但答案是 41。
        // 這強制我們必須認為：離開時間 = 下一個病人的到達時間（如果該病人導致超時）？
        // 或者，離開時間 = last_end + 30 + 1？
        // 如果 last_end = 10, 10+30+1 = 41。這符合範例。
        // 如果病人是 100 來，last_end = 10. 10+30+1 = 41. 醫生在 41 離開。這合理，因為 11~41 已經超過 30 分鐘沒人（實際是 31 分鐘跨度？）。
        // 讓我們計算分鐘數：
        // 11, 12, ..., 40. 這是 30 個分鐘點。
        // 如果在第 40 分鐘結束時，已經過了 30 分鐘沒人。
        // 題目說「不含三十分鐘整」，意思是間隔時間 > 30。
        // 間隔時間 = A - last_end.
        // 如果 A=40, 間隔=30. 不離開。
        // 如果 A=41, 間隔=31. 離開。
        // 離開時間點：範例說是 41。
        // 這強烈暗示：如果發生超時，離開時間 = last_end + 30 + 1？
        // 或者更簡單的：離開時間 = 下一個病人的到達時間？
        // 如果病人是 100 來，醫生會等到 100 才走嗎？
        // 題目說「若連續三十分鐘...沒有病患...便結束」。這是一個自動觸發機制，不应该依赖病人是否出現。
        // 所以醫生應該在 last_end + 30 之後的某個點離開。
        // 如果範例是 41，而 last_end 是 10。那離開時間是 41。
        // 這等於 last_end + 31？
        // 或者，題目對於「分鐘」的定義是離散的？
        // 第 11 分鐘沒人，第 12 分鐘沒人... 第 40 分鐘沒人。
        // 到了第 41 分鐘，發現已經連續 30 分鐘（11~40）没人了，所以在第 41 分鐘離開。
        // 這樣邏輯就通了：醫生在每一分鐘開始時檢查過去 30 分鐘是否有人。
        // 在第 41 分鐘開始時，檢查 11~40，發現沒人，於是離開。
        // 所以離開時間 = last_end + 30 + 1？
        // 讓我們用這個邏輯：如果 A - last_end > 30，則離開時間 = last_end + 30 + 1。
        // 但如果 A 很小，比如 A = last_end + 31，那醫生在 last_end + 31 離開？
        // 如果 A = 100，醫生在 41 離開。
        // 這樣的話，程式邏輯應該是：
        // 遍歷病人，如果發現當前病人到達時間 A > last_end + 30，則醫生在 last_end + 30 + 1 離開，break。
        // 如果所有病人都看完了，醫生什麼時候離開？
        // 題目沒說看完所有病人後要不要等。
        // 但根據規則 4，只要連續 30 分鐘沒人就走。
        // 看完最後一個病人後，如果沒人再來，那也會觸發規則 4。
        // 所以最後一個病人看完後，離開時間 = last_end + 30 + 1？
        // 但題目只給了有病人的情況。
        // 讓我們再看範例。範例中是因為有新病人來但超時了，所以輸出 41。
        // 如果輸入只有兩個病人 1, 3。沒有 41。
        // 醫生在 10 分看完。之後沒人。
        // 他會在 10+30+1 = 41 分離開嗎？
        // 題目描述「若連續三十分鐘...沒有病患候診，便結束」。
        // 這應該適用於任何情況，包括最後一個病人看完後。
        // 所以無論如何，離開時間都是最後一個有效看診結束時間 + 31？
        // 等等，如果中間有插隊？不，規則說不能插隊。
        // 讓我們修正邏輯：
        // 模擬時間推進。
        // 當前時間 cur。最後結束時間 last_end。
        // 下一個病人到達時間 next_arrival。
        // 如果 next_arrival > last_end + 30:
        //    醫生在 last_end + 30 + 1 離開。
        //    return last_end + 30 + 1.
        // 否則:
        //    醫生看診。
        //    start_time = max(cur, next_arrival). 但因為是排隊，cur 應該就是 last_end (如果沒等待) 或 next_arrival (如果有等待但沒超時).
        //    實際上，如果 next_arrival <= last_end + 30:
        //       如果 next_arrival >= last_end: 醫生空等了一會兒，然後在 next_arrival 開始看，結束於 next_arrival + 5.
        //       如果 next_arrival < last_end: 醫生在 last_end 開始看，結束於 last_end + 5.
        //       更新 last_end.
        // 如果所有病人都處理完了，醫生在 last_end + 30 + 1 離開。
        
        // 讓我們用這個邏輯測試範例 1:
        // N=3, arrivals = {1, 3, 41}
        // i=0, arr=1. last_end=0 (初始). 1 > 0+30? No.
        // start = max(0, 1) = 1. end = 6. last_end = 6. (等等，題目範例說是 1-5 分鐘？)
        // 題目範例：「第 1 位病患 (第 1 至 5 分鐘)」。
        // 這表示看診時間是 [start, start+4]? 還是 [start, start+5)?
        // 「一律為五分鐘整」。1, 2, 3, 4, 5. 這是 5 分鐘。結束時間是 5.
        // 所以 end = start + 5 - 1? 或者 end_time 定義為「第幾分鐘結束」？
        // 如果 1 至 5 分鐘，那結束時刻是 5. 下一個可以開始的時刻是 6.
        // 所以 duration = 5. end_time = start_time + 5.
        // 範例說：第 1 至 5 分鐘 (病人 1), 第 6 至 10 分鐘 (病人 2).
        // 所以病人 1: start=1, end=5. (佔用 1,2,3,4,5)
        // 病人 2: start=6, end=10.
        // 我的代碼裡 current_time 應該代表「下一個可用的開始時間」。
        // 初始 current_time = 0? 不，醫生 0 分到，但第一個病人 1 分到。
        // 所以第一個病人 start = max(0, 1) = 1. end = 1+5 = 6? 
        // 但範例說 1-5. 這表示 end_time 是 5.
        // 如果 end_time 是 5，那下一個開始時間是 6.
        // 所以公式應該是：end_time = start_time + 5 - 1? 
        // 或者，時間是連續的？
        // 「第 1 至 5 分鐘」通常包含 1,2,3,4,5. 長度 5.
        // 結束時刻是 5. 下一個時刻是 6.
        // 所以如果 start=1, end=5. next_start = 6.
        // 那麼 end = start + 4?
        // 不，這取決於怎麼定義「第幾分鐘離開」。
        // 如果醫生在 5 分鐘結束看診，那他在 5 分鐘時還在嗎？
        // 範例說「第 1 至 5 分鐘」，然後「第 6 至 10 分鐘」。
        // 這暗示時間軸是離散的分鐘點。
        // 讓我們假設：
        // start_time = arrival_time (如果醫生有空)
        // end_time = start_time + 5 - 1 = start_time + 4.
        // 下一個可用時間 = end_time + 1 = start_time + 5.
        // 這樣：
        // P1: arr=1. start=1. end=5. next_avail=6.
        // P2: arr=3. 3 < 6. 所以要等。start=6. end=10. next_avail=11.
        // P3: arr=41. 41 >= 11.
        // 檢查間隔：41 - 10 = 31. (last_end=10).
        // 31 > 30. 超時。
        // 離開時間 = last_end + 30 + 1 = 10 + 30 + 1 = 41.
        // 符合範例！
        
        // 所以邏輯確認：
        // 1. 每個病人看診 5 分鐘。如果 start 在 S，則結束在 S+4。下一個可用時間是 S+5。
        // 2. 如果下一個病人到達時間 A > last_end + 30 (其中 last_end 是上一個病人的結束時間)，則醫生在 last_end + 30 + 1 離開。
        // 3. 如果所有病人都看完，醫生在 last_end + 30 + 1 離開。
        
        // 變數定義：
        // last_end: 上一個病人結束的分鐘數。初始為 0? 
        // 但醫生 0 分到。如果第一個病人 1 分到。
        // 間隔是 1 - 0 = 1 <= 30. 不看錯。
        // start = max(last_end + 1, arrival)? 
        // 不，如果 last_end=0 (還沒看任何人), 下一個可用時間是 0? 
        // 醫生 0 分到，隨時可以看。
        // 如果病人 0 分到？題目說正整數，所以最小 1.
        // 所以第一個病人：
        // last_end 初始化為 0.
        // next_avail = last_end + 1? 如果 last_end=0, next_avail=1.
        // 但醫生 0 分就到了，如果病人 1 分來，醫生從 1 開始看。
        // 如果病人 0 分來（雖然題目說正整數），醫生從 0 開始看？
        // 題目說「第 0 分鐘時抵達診所」。病人是「正整數」。
        // 所以第一個病人最早 1 分來。
        // 此時 last_end = 0. next_avail = 1 (因為 0 分沒事做，1 分開始看).
        // 其實 next_avail 應該是 max(last_end + 1, 0)? 
        // 讓我們統一：
        // current_start_time: 醫生可以開始看下一個病人的最早時間。
        // 初始 current_start_time = 0 (醫生 0 分到).
        // 但病人最早 1 分到。
        // 對於每個病人 arrival:
        //   如果 arrival >= current_start_time:
        //      間隔 = arrival - (current_start_time - 1)? 
        //      不，間隔應該是相對於「上一次結束時間」。
        //      讓我們維護 last_end_time.
        //      初始 last_end_time = 0? 但 0 分沒看病。
        //      其實第一次沒有「上一次結束」。
        //      我們可以特殊處理第一個病人，或者設 last_end_time = 0, 並約定間隔計算方式。
        //      如果 last_end_time = 0, arrival = 1. 間隔 = 1 - 0 = 1. OK.
        //      看診：start = max(last_end_time + 1, arrival).
        //      如果 arrival >= last_end_time + 1: start = arrival.
        //      end = start + 4.
        //      last_end_time = end.
        //   如果 arrival < current_start_time (即 arrival <= last_end_time):
        //      排隊。start = last_end_time + 1.
        //      end = start + 4.
        //      last_end_time = end.
        //   
        //   關鍵檢查：在決定看診前，如果 arrival > last_end_time + 30:
        //      離開時間 = last_end_time + 30 + 1.
        //      break.
        
        // 重測範例：
        // last_end = 0.
        // P1: arr=1. 1 > 0+30? No.
        // start = max(0+1, 1) = 1.
        // end = 1+4 = 5.
        // last_end = 5.
        // P2: arr=3. 3 > 5+30? No.
        // 3 < 5+1 (6). 所以排隊。
        // start = 5+1 = 6.
        // end = 6+4 = 10.
        // last_end = 10.
        // P3: arr=41. 41 > 10+30 (40)? Yes.
        // 離開時間 = 10 + 30 + 1 = 41.
        // Break.
        // 輸出 41. 正確。
        
        // 另一個測試：假如 P3 是 40.
        // 40 > 40? No.
        // start = max(11, 40) = 40.
        // end = 44.
        // last_end = 44.
        // 沒更多病人。
        // 離開時間 = 44 + 30 + 1 = 75.
        
        // 這看起來合理。
        
        int final_leave_time = 0;
        bool left_early = false;
        
        // 初始 last_end_time. 
        // 醫生 0 分到。如果第一個病人 1 分到。
        // 我們可以把 last_end_time 設為 0，代表 0 分時「閒置狀態結束」？
        // 或者更準確地，last_end_time 代表「最後一個動作結束的時間」。
        // 初始時，醫生剛到，還沒看任何人。
        // 我們可以認為 last_end_time = 0.
        // 第一個病人來時，間隔 = arrival - 0.
        // 如果 arrival > 30, 醫生直接走？
        // 題目說「若連續三十分鐘...沒有病患候診」。
        // 醫生 0 分到。如果第一個病人 31 分到。
        // 0~30 這 30 分鐘沒人（1~30 沒人？）。
        // 0 分醫生到，1,2,...,30 沒人。31 人來。
        // 間隔 31-0 = 31 > 30. 醫生走。
        // 離開時間 = 0 + 30 + 1 = 31.
        // 這合理嗎？
        // 醫生 0 分到，等到 30 分（共 30 分鐘沒人），31 分走。
        // 是的。
        
        int last_end = 0; 
        
        for (int i = 0; i < N; ++i) {
            int arr = arrivals[i];
            
            // 檢查是否超時
            if (arr > last_end + 30) {
                final_leave_time = last_end + 30 + 1;
                left_early = true;
                break;
            }
            
            // 計算開始時間
            int start_time = (arr > last_end) ? arr : (last_end + 1);
            int end_time = start_time + 4;
            last_end = end_time;
        }
        
        if (!left_early) {
            final_leave_time = last_end + 30 + 1;
        }
        
        cout << final_leave_time << endl;
    }
    return 0;
}

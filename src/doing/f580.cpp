#include <iostream>
#include <vector>

int main() {
    int n, m, a, b;
    std::cin >> n >> m;
    std::vector<int> ans(n);
    for (int i=0; i<m; i++) {
        std::cin >> a >> b;
        switch (b) {
            case -1:
                break;
            case -2:
                break;
        }
    }

    return 0;
}